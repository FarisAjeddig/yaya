{% extends 'base.html.twig' %}
{% block body %}
    {% embed 'admin/base.html.twig' %}
        {% block secondTitle %}
            Admin - Supprimer {{ user.username }}
        {% endblock %}
        {% block bodyAdmin %}
            <div class="container-fluid row p-5">
                <div class="col-lg-3"></div>
                <div class="col-lg-6 text-center">
                    {{ user.username }}<br>
                    {{ user.email }} <br>
                    {{ user.phone_number }} <br><br>
                    <a href="{{ url('admin_confirm_delete_user', {id: user.id}) }}" class="genric-btn primary e-large" style="font-size: 20px ">Confirmer la suppression</a>
                </div>
            </div>
        {% endblock %}
    {% endembed %}
{% endblock %}